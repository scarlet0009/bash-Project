LOG_FILE=""
THRESHOLD=5
WINDOW_SECONDS=120
BLOCK_DURATION_SECONDS=86400
BLOCKER="iptables"

DATA_DIR="/var/log/smartlogwatcher"
RUNTIME_DIR="/var/run/smartlogwatcher"
REPORT_DIR="${DATA_DIR}/reports"

BLOCKED_FILE="${DATA_DIR}/blocked_ips.csv"
SUSPICIOUS_LOG="${DATA_DIR}/suspicious.log"

FAIL_PATTERNS="Failed password|Invalid user|authentication failure"

DRY_RUN=0
